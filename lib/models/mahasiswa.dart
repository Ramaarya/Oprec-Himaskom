class Mahasiswa {
  String id, name, nim, bidang, imageUrl;

  Mahasiswa(
      {required this.id,
      required this.name,
      required this.nim,
      required this.bidang,
      required this.imageUrl});
}
